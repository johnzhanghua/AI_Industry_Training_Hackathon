# Credentials -- the submission repo must be fully public, so never commit these.
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/

# OS noise
.DS_Store

# generated/staged data -- regenerate with src/data_prep/*.py, don't commit hundreds of MB
/data/
/training/data/
